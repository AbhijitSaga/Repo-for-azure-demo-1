# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - demo-java-docker-aap

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://myfirstregistrydemo.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_b63fc9289728438e8b14e41bf7b25aea }}
        password: ${{ secrets.AzureAppService_ContainerPassword_43c1c042c17b4fb594aaf6acd93c6f86 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: myfirstregistrydemo.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_b63fc9289728438e8b14e41bf7b25aea }}/mcr.microsoft.com/appsvc/staticsite:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'demo-java-docker-aap'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_e69812bed2b148a0b6293152e3eccb76 }}
        images: 'myfirstregistrydemo.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_b63fc9289728438e8b14e41bf7b25aea }}/mcr.microsoft.com/appsvc/staticsite:${{ github.sha }}'